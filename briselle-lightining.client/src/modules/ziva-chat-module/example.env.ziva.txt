# =============================================================================
# Ziva module — copy to `.env.ziva` in this folder (ziva-chat-module/)
# Or set the same keys in the host app / shell environment.
# =============================================================================

# --- Standalone Ziva API (plug-and-play; run: npm run server) ---
ZIVA_HOST=127.0.0.1
ZIVA_PORT=5199

# Groq (required for AI field suggestions and general chat)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Optional branding for /api/ziva Q&A
ZIVA_PRODUCT_NAME=Briselle
ZIVA_BASE_SITE_URL=https://www.briselle.com
# ZIVA_CONTEXT_MARKDOWN=## My product\n...

# Optional: verify Ziva + Groq reachability (browser or curl)
# GET http://127.0.0.1:5199/GroqStatusCheck

# Browser CORS when host UI runs on another origin (e.g. Vite :5173)
# ZIVA_CORS_ORIGIN=http://localhost:5173

# =============================================================================
# Host app (Briselle-Lightining.client) — copy into project root `.env`
# =============================================================================
# Point the React widget at the standalone Ziva server:
VITE_ZIVA_API_URL=http://127.0.0.1:5199/api/ziva

# Vite dev only: proxy /api/ziva → Ziva server (if you prefer relative URLs)
# VITE_ZIVA_API_URL=/api/ziva
# VITE_ZIVA_PROXY_TARGET=http://127.0.0.1:5199

# ZIVA AI (optional - for AI chat answers via Groq)
GROQ_API_KEY=