Place static files here in your **host** app (not necessarily inside this module zip):

- `briselle-logo.png` — avatar, FAB, hero
- `ziva_sparkle_white.svg` — logo sparkle and FAB accent

Default paths in `defaultConfig.js` are `/assets/briselle-logo.png` and `/assets/ziva_sparkle_white.svg`.
