For all designs I ask you to make, have them be beautiful,  cookie cutter. Make webpages that are fully featured and worthy for production.

By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, etc unless absolutely necessary or I request them.

Use icons from lucide-react for logos.

Implement styling in React (Tailwind classes), not separate global theme CSS files.

Templatize and reuse UI as components, following patterns similar to the Lightning Design System component set: https://www.lightningdesignsystem.com/2e1ef8501/p/755aff-components