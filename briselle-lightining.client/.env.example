# Briselle Lightning client — environment template

# Ziva plug-in: URL of the standalone Ziva API (run ziva-chat-module `npm run server`)
VITE_ZIVA_API_URL=http://127.0.0.1:5199/api/ziva

# Vite dev proxy target when using VITE_ZIVA_API_URL=/api/ziva
VITE_ZIVA_PROXY_TARGET=http://127.0.0.1:5199
